import { styled } from "@mui/material";
import MuButton from "@mui/material/Button";

export const MUIButton = styled(MuButton)`
  font-style: normal;
  font-weight: bold;
  font-size: 14px;
  line-height: 24px;
  box-shadow: none;
  text-decoration: none;
  text-transform: capitalize;
`;
